import React from 'react';

const FOLLOW = "FOLLOW";

let initialState = {
    userId: null,
    email: null,
    login: null
}

const authReducer = (state = initialState, action) => {

    return state;
}

export default authReducer;